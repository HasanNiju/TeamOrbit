{
  "name": "team-orbit",
  "private": true,
  "version": "1.0.0",
  "description": "TeamOrbit Employee Reporting System — monorepo (employee frontend + backend API + admin panel), deployable as a single service.",
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend"
  },
  "engines": {
    "node": ">=18"
  }
}
